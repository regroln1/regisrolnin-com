---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const resources = [
  {
    title: "Guide Clarté AIA™ – 5 questions pour aligner votre offre",
    description: "Un mini-guide pour clarifier votre positionnement, renforcer votre message et détecter les angles morts de votre offre.",
    benefits: [
      "Gagnez en clarté stratégique",
      "Identifiez les blocages invisibles",
      "Alignez votre offre avec les attentes de vos clients"
    ],
    cta: "Télécharger le Guide Clarté",
    link: "/ressources/guide-clarte-aia",
    color: "amber",
    icon: "checklist"
  },
  {
    title: "Kit d’Acquisition AIA™ – Attirez plus de clients qualifiés",
    description: "Des modèles et stratégies pour renforcer votre acquisition et attirer les bons prospects, sans dépendre uniquement du bouche-à-oreille.",
    benefits: [
      "Structurez un moteur d’acquisition fiable",
      "Déclenchez l’intérêt des bons clients",
      "Gagnez en visibilité avec moins d’effort"
    ],
    cta: "Télécharger le Kit d’Acquisition",
    link: "/ressources/kit-acquisition-aia",
    color: "blue",
    icon: "template"
  },
  {
    title: "Flash Audit AIA™ – Score d'automatisation en 10 min",
    description: "Un auto-diagnostic simple pour évaluer la maturité de votre système interne et identifier les process à automatiser en priorité.",
    benefits: [
      "Évaluez vos points de friction",
      "Gagnez du temps sans perdre en qualité",
      "Préparez votre entreprise à scaler sereinement"
    ],
    cta: "Lancer le Flash Audit",
    link: "/ressources/flash-audit-aia",
    color: "purple",
    icon: "audit"
  }
];

---

<Layout 
  title="Ressources Gratuites pour PME - Growth Expert"
  description="Des outils pratiques et actionnables pour clarifier votre stratégie, structurer votre acquisition et préparer votre automatisation."
  type="website"
>
  <Header />
  
  <main class="pt-20 bg-gradient-to-br from-gray-50 via-white to-blue-50 min-h-screen">
    <!-- Hero Section -->
    <section class="py-20 lg:py-32 relative overflow-hidden">
      <!-- Floating background elements -->
      <div class="absolute top-20 left-10 w-64 h-64 bg-blue-500/5 rounded-full blur-3xl float-slow"></div>
      <div class="absolute bottom-20 right-10 w-80 h-80 bg-purple-500/5 rounded-full blur-3xl float-medium"></div>
      <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-48 h-48 bg-amber-500/5 rounded-full blur-2xl float-fast"></div>

      <div class="relative z-10 container mx-auto px-6 text-center">
        <div class="max-w-4xl mx-auto">
          <!-- Badge -->
          <div class="inline-flex items-center px-6 py-3 bg-white/80 backdrop-blur-sm border border-blue-200/50 rounded-full mb-8 animate-fade-in shadow-sm">
            <svg class="w-5 h-5 mr-3 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 01-2-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
            <span class="text-sm font-semibold text-blue-600">Ressources Exclusives</span>
          </div>

          <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-navy-900 mb-8 font-crimson animate-fade-in leading-tight" style="animation-delay: 0.1s;">
            Ressources gratuites pour accélérer
            <span class="text-gradient-gold block">la croissance de votre PME</span>
          </h1>

          <p class="text-xl text-gray-600 mb-12 font-crimson leading-relaxed animate-fade-in max-w-3xl mx-auto" style="animation-delay: 0.2s;">
            Des outils pratiques et actionnables pour clarifier votre stratégie, structurer votre acquisition et préparer votre automatisation.
          </p>

          <!-- Intro text -->
          <div class="bg-white/60 backdrop-blur-sm border border-white/30 rounded-2xl p-8 mb-16 animate-fade-in" style="animation-delay: 0.3s;">
            <p class="text-lg text-gray-700 font-crimson">
              Ces guides, checklists et audits sont conçus pour vous aider à franchir un cap dans votre business – 
              <strong>sans jargon, sans complexité inutile.</strong>
            </p>
          </div>

          <a
            href="#resources"
            class="inline-flex items-center px-10 py-4 bg-gradient-to-r from-blue-600 to-purple-600 text-white font-semibold rounded-full hover:shadow-xl hover:-translate-y-1 transition-all duration-300 text-lg animate-fade-in"
            style="animation-delay: 0.4s;"
          >
            Accédez à vos ressources maintenant
            <svg class="ml-3 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
            </svg>
          </a>
        </div>
      </div>
    </section>

    <!-- Resources Section -->
    <section id="resources" class="py-20 lg:py-32">
      <div class="container mx-auto px-6">
        <div class="max-w-7xl mx-auto">
          <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {resources.map((resource, index) => (
              <div
                class={`bg-white/80 backdrop-blur-sm border border-white/30 rounded-3xl p-8 shadow-xl hover:shadow-2xl hover:-translate-y-2 transition-all duration-500 ${
                  index === 0 ? 'animate-slide-left' : 
                  index === 1 ? 'animate-fade-in' : 'animate-slide-right'
                }`}
                style={`animation-delay: ${0.2 + index * 0.2}s`}
              >
                <!-- Icon -->
                <div class={`w-20 h-20 rounded-3xl flex items-center justify-center mx-auto mb-8 ${
                  resource.color === 'amber' ? 'bg-gradient-to-r from-amber-500 to-amber-600' :
                  resource.color === 'blue' ? 'bg-gradient-to-r from-blue-500 to-blue-600' :
                  'bg-gradient-to-r from-purple-500 to-purple-600'
                }`}>
                  {resource.icon === 'checklist' && (
                    <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 01-2-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                    </svg>
                  )}
                  {resource.icon === 'template' && (
                    <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                    </svg>
                  )}
                  {resource.icon === 'audit' && (
                    <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
                    </svg>
                  )}
                </div>

                <!-- Title -->
                <h2 class="text-2xl font-bold text-navy-900 mb-4 font-crimson text-center">
                  {resource.title}
                </h2>

                <!-- Description -->
                <p class="text-gray-600 mb-8 font-crimson text-center leading-relaxed">
                  {resource.description}
                </p>

                <!-- Benefits -->
                <div class="space-y-3 mb-8">
                  {resource.benefits.map((benefit) => (
                    <div class="flex items-center">
                      <svg class="w-5 h-5 text-green-500 mr-3 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                      </svg>
                      <span class="text-gray-700 font-crimson">✔ {benefit}</span>
                    </div>
                  ))}
                </div>

                <!-- CTA Button -->
                <a
                  href={resource.link}
                  target="_blank"
                  rel="noopener noreferrer"
                  class={`block w-full text-center px-6 py-4 text-white font-bold rounded-xl hover:shadow-xl hover:-translate-y-1 transition-all duration-300 ${
                    resource.color === 'amber' ? 'bg-gradient-to-r from-amber-500 to-amber-600' :
                    resource.color === 'blue' ? 'bg-gradient-to-r from-blue-500 to-blue-600' :
                    'bg-gradient-to-r from-purple-500 to-purple-600'
                  }`}
                  onclick="if (typeof window !== 'undefined' && window.trackConversion) { window.trackConversion.leadMagnetDownloaded('${resource.title}'); }"
                >
                  {resource.cta}
                </a>
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>

    <!-- Footer Section -->
    <section class="py-16">
      <div class="container mx-auto px-6">
        <div class="max-w-4xl mx-auto text-center">
          <!-- Trust indicator -->
          <div class="bg-white/60 backdrop-blur-sm border border-white/30 rounded-2xl p-8 mb-12">
            <h3 class="text-2xl font-bold text-navy-900 mb-4 font-crimson">
              100% Gratuit, 0% Spam
            </h3>
            <p class="text-lg text-gray-700 font-crimson leading-relaxed">
              Toutes ces ressources sont <strong>100% gratuites</strong>. Pas de spam, uniquement des outils utiles pour faire progresser votre business.
            </p>
          </div>

          <!-- Next step CTA -->
          <div class="bg-gradient-to-r from-blue-600 to-purple-600 rounded-2xl p-8 text-white">
            <h3 class="text-2xl font-bold mb-4 font-crimson">
              Prêt pour l'étape suivante ?
            </h3>
            <p class="text-lg mb-6 font-crimson opacity-90">
              Réservez votre session stratégique gratuite et découvrez comment appliquer ces outils à votre situation spécifique.
            </p>
            <a
              href="/travailler-avec-moi"
              class="inline-flex items-center px-8 py-4 bg-white text-blue-600 font-bold rounded-xl hover:bg-blue-50 hover:shadow-xl hover:-translate-y-1 transition-all duration-300"
            >
              Réserver ma session stratégique
              <svg class="ml-2 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
              </svg>
            </a>
          </div>
        </h1>
      </div>
    </section>
  </main>
  
  <Footer />
</Layout>